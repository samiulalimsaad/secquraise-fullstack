import LeftSidebar from "./LeftSidebar";

const Layout = () => {
    return (
        <div className="h-[calc(100vh-4rem)]">
            <LeftSidebar />
        </div>
    );
};

export default Layout;
